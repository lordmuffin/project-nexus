# Maestro E2E Test: Connection and Pairing Flow
appId: com.nexus.companion
---
- launchApp
- assertVisible: "Nexus Companion"

# Test connection status display
- assertVisible: "Disconnected"
- assertVisible: "🔴"

# Test QR scanner flow
- tapOn: "Scan QR Code"
- assertVisible: "Point camera at QR code"
- assertVisible: "Cancel"
- tapOn: "Cancel"

# Test manual connection flow
- tapOn: "Connect Manually" 
- assertVisible: "Enter the IP address"
- inputText: "192.168.1.100"
- tapOn: "Connect"

# Wait for potential connection attempt
- waitForAnimationToEnd

# Test settings navigation
- tapOn: "Settings"
- assertVisible: "Device Settings"
- assertVisible: "Connection"
- goBack

# Verify we're back on home screen
- assertVisible: "Start Recording"
- assertVisible: "Scan QR Code"